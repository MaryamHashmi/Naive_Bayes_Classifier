# Naive_Bayes_Classifier

Python implementation of naive bayesian classifier.

test_data.py and training_data.py initialize input in the form of lists of lists

main.py uses training data to train classifier from naive_bayesian_classifier.py and applies Laplace Smoothing technique

The end result is label(status of employee in this example) 
